vendor: hp
entries:
  - id: hp_1
    model: HP-Series-1
    symptoms: "Intermittent printing issue 1, queue stalls, and occasional offline state."
    probable_cause: "Driver mismatch (PCL vs PS), stale queue jobs, or print server mapping drift."
    recommended_fix_steps:
      - "Confirm vendor driver package matches device model."
      - "If port is WSD and unstable, move to Standard TCP/IP."
      - "Clear queue and restart spooler service."
      - "Validate print server share permissions and reconnect."
  - id: hp_2
    model: HP-Series-2
    symptoms: "Intermittent printing issue 2, queue stalls, and occasional offline state."
    probable_cause: "Driver mismatch (PCL vs PS), stale queue jobs, or print server mapping drift."
    recommended_fix_steps:
      - "Confirm vendor driver package matches device model."
      - "If port is WSD and unstable, move to Standard TCP/IP."
      - "Clear queue and restart spooler service."
      - "Validate print server share permissions and reconnect."
  - id: hp_3
    model: HP-Series-3
    symptoms: "Intermittent printing issue 3, queue stalls, and occasional offline state."
    probable_cause: "Driver mismatch (PCL vs PS), stale queue jobs, or print server mapping drift."
    recommended_fix_steps:
      - "Confirm vendor driver package matches device model."
      - "If port is WSD and unstable, move to Standard TCP/IP."
      - "Clear queue and restart spooler service."
      - "Validate print server share permissions and reconnect."
  - id: hp_4
    model: HP-Series-4
    symptoms: "Intermittent printing issue 4, queue stalls, and occasional offline state."
    probable_cause: "Driver mismatch (PCL vs PS), stale queue jobs, or print server mapping drift."
    recommended_fix_steps:
      - "Confirm vendor driver package matches device model."
      - "If port is WSD and unstable, move to Standard TCP/IP."
      - "Clear queue and restart spooler service."
      - "Validate print server share permissions and reconnect."
  - id: hp_5
    model: HP-Series-5
    symptoms: "Intermittent printing issue 5, queue stalls, and occasional offline state."
    probable_cause: "Driver mismatch (PCL vs PS), stale queue jobs, or print server mapping drift."
    recommended_fix_steps:
      - "Confirm vendor driver package matches device model."
      - "If port is WSD and unstable, move to Standard TCP/IP."
      - "Clear queue and restart spooler service."
      - "Validate print server share permissions and reconnect."
  - id: hp_6
    model: HP-Series-6
    symptoms: "Intermittent printing issue 6, queue stalls, and occasional offline state."
    probable_cause: "Driver mismatch (PCL vs PS), stale queue jobs, or print server mapping drift."
    recommended_fix_steps:
      - "Confirm vendor driver package matches device model."
      - "If port is WSD and unstable, move to Standard TCP/IP."
      - "Clear queue and restart spooler service."
      - "Validate print server share permissions and reconnect."
  - id: hp_7
    model: HP-Series-7
    symptoms: "Intermittent printing issue 7, queue stalls, and occasional offline state."
    probable_cause: "Driver mismatch (PCL vs PS), stale queue jobs, or print server mapping drift."
    recommended_fix_steps:
      - "Confirm vendor driver package matches device model."
      - "If port is WSD and unstable, move to Standard TCP/IP."
      - "Clear queue and restart spooler service."
      - "Validate print server share permissions and reconnect."
  - id: hp_8
    model: HP-Series-8
    symptoms: "Intermittent printing issue 8, queue stalls, and occasional offline state."
    probable_cause: "Driver mismatch (PCL vs PS), stale queue jobs, or print server mapping drift."
    recommended_fix_steps:
      - "Confirm vendor driver package matches device model."
      - "If port is WSD and unstable, move to Standard TCP/IP."
      - "Clear queue and restart spooler service."
      - "Validate print server share permissions and reconnect."
  - id: hp_9
    model: HP-Series-9
    symptoms: "Intermittent printing issue 9, queue stalls, and occasional offline state."
    probable_cause: "Driver mismatch (PCL vs PS), stale queue jobs, or print server mapping drift."
    recommended_fix_steps:
      - "Confirm vendor driver package matches device model."
      - "If port is WSD and unstable, move to Standard TCP/IP."
      - "Clear queue and restart spooler service."
      - "Validate print server share permissions and reconnect."
  - id: hp_10
    model: HP-Series-10
    symptoms: "Intermittent printing issue 10, queue stalls, and occasional offline state."
    probable_cause: "Driver mismatch (PCL vs PS), stale queue jobs, or print server mapping drift."
    recommended_fix_steps:
      - "Confirm vendor driver package matches device model."
      - "If port is WSD and unstable, move to Standard TCP/IP."
      - "Clear queue and restart spooler service."
      - "Validate print server share permissions and reconnect."
